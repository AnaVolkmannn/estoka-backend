spring:
  application:
    name: diorana
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: estoka123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE